<!-- <div class="dropdown clearfix pull-right">
    <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
      排序
        <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
        <li>
          <%= link_to("按照薪资下限排序", jobs_path(:order => "by_lower_bound")) %>
        </li>
        <li>
            <%= link_to("按照薪资上限排序", jobs_path(:order => "by_upper_bound")) %>

        </li>
        <li>
          <%= link_to("按照发表时间排序", jobs_path ) %>

        </li>
    </ul>
</div> -->
<div class="tpull-right">
  <%=link_to("发布新欢",new_admin_job_path,:class=>"btn btn-default")%>
</div>
<table class="table table-boldered">
  <thead>
      <tr>
          <td>What are you zuijinfan？</td>
          <td></td>
          <!-- <td>薪资下限</td>
          <td>薪资上限</td> -->
          <td>发布时间</td>
      </tr>

  </thead>
  <tbody>
      <% @jobs.each do |job| %>
      <tr>
          <td>
              <%= link_to(job.user.email, chaxun_job_path(job),:method => :get)%>最近FAN<%= link_to(job.title, job_path(job))%><%=simple_format(job.description)%>
          </td>
          <!-- <td>
              <%= job.wage_lower_bound %>
          </td>
          <td>
              <%= job.wage_upper_bound %>
          </td> -->
          <td>
            <%= link_to("评论", new_job_resume_path(job), class: "btn btn-default pull-right")%>
          </td>
          <td>
              <%= job.created_at %>
          </td>
      </tr>
      <% end %>
  </tbody>


</table>
