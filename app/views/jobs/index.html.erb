<div class="col-md-12">

  <table class="table table-hover">
    <tbody>
      <% @jobs.each do |job| %>
        <tr>
          <td><%= link_to(job.title, job_path(job)) %></td>

          <td><%= job.created_at %></td>
        </tr>
        <% end %>
    </tbody>
  </table>
</div>
