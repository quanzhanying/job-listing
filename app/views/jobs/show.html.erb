<div class="col-md-12">

  <div class="job">
    <%= link_to("返回主页", jobs_path, class: "btn btn-primary pull-left")%>
  </div>

  <br><br>

  <h2><%= @job.title %></h2>
  <p><%= simple_format(@job.description) %></p>
</div>

<div class="text-center">
  <%= link_to("投交履历", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
</div>
