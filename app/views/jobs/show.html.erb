<div class="col-md-12">
  <div class="job">
    <%= link_to("Add a job", new_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>
</div>
