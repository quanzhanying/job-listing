<div class="text-center">
  <%= link_to("Submit Resume", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
</div>
<h1> <%= @job.title %> </h1>
<p>
  <%= simple_format(@job.description) %>
</p>
