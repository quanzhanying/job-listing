<div class="col-md-12">
  <div class="job_params">
    <%= link_to("上传简历", new_job_resume_path(@job), class: "btn btn-default pull-right")%>

    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @job.title %></h2>
  <p><%= simple_format(@job.description) %></p>
</div>
