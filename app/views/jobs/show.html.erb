<div class="col-md-12">
  <% if current_user %>
  <div class="job">
    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <% end %>
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>
</div>
