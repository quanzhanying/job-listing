<div class="col-md-12">

  <p>👤岗位(标题):</p>
  <h2><%= @job.title %></h2>

  <hr>

  <p>🔎详情(描述):</p>
  <h4><%= simple_format(@job.description) %></h4>

  <br>

  <p>💰薪资下限~上限</p>
  <h4><%= @job.wage_lower_bound %> ~ <%= @job.wage_upper_bound %></h4>

  <br>

  <p>📬联系邮箱</p>
  <h4><%= @job.contact_email %></h4>

  <div class="text-center">
    <%= link_to("[投交简历]", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
  </div>

  <div class="jobs_back">
    <%= link_to("🔙返回", jobs_path, class: "btn btn-success pull-right")%>
  </div>
  <!--<div class="job_edit">
    <%= link_to("📝编辑", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>-->
</div>
