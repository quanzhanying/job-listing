<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>
  <p><%= @job.wage_lower_bound %>
  <p><%= @job.wage_upper_bound %>
  <p><%= @job.contact_email %>

  <div class="text-center">
    <%= link_to("投交履歷", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
  </div>
</div>
