<div class="col-md-12">
  <!-- <div class="group">
    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div> -->
  <table class="table table-bordered">
    <thead>
      <tr>

        <td class="bg-success">description</td>
        <td class="bg-success">薪资下限</td>
        <td class="bg-success">薪资上限</td>
        <td class="bg-success">Time</td>
        <td class="bg-success">Email</td>

      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @job.description %></td>
          <td><%= @job.wage_lower_bound %></td>
          <td><%= @job.wage_upper_bound %></td>
          <td><%= @job.created_at %> </td>
          <td><%= @job.contact_email %></td>
        </tr>
      </tbody>
    </table>
</div>
