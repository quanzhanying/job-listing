<h1> <% @job.title %></h1>
<div class="pull-right">
	<% if current_user && !current_user.admin? %>
        <%= link_to("Submit your Resume", new_job_resume_path(@job), :class => "btn btn-default" ) %>
      <% end %>
</div>
<p>
  <%= simple_format(@job.description)%>
</p>
