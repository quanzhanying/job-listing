<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-default pull-right")%>
  </div>
  <h2><%= @job.title%></h2>
  <p>
    <%= simple_format(@job.description)%>
  </p>
</div>
