</div>
<div class="container-fluid juzhong">

  <table class="table table-boldered">
    <thead>
        <tr>
            <td>我的“新欢”</td>
            <td></td>
            <!-- <td>薪资下限</td>
            <td>薪资上限</td> -->
            <td>发布时间</td>
        </tr>

    </thead>
    <tbody>
        <% @jobs.each do |job| %>
        <tr>
            <td>
                最近FAN<%= link_to(job.title, job_path(job))%><%=simple_format(job.description)%>
            </td>
            <!-- <td>
                <%= job.wage_lower_bound %>
            </td>
            <td>
                <%= job.wage_upper_bound %>
            </td> -->
            <td>
              <%= link_to("Delete", job_path(job), :method => :delete,:class => "btn btn-xs btn-danger",:data=>{:confirm=>"Are you sure?"})%>
              <% if job.is_hidden %>

                <%= link_to("Publish", publish_admin_job_path(job) , :method => :post, :class => "btn btn-xs btn-success") %>
              <% else %>
                <%= link_to("Hide", hide_admin_job_path(job), :method => :post, :class => "btn btn-xs btn-warning") %>
              <% end %>
              <%= link_to("评论", new_job_resume_path(job), class: "btn btn-default pull-right")%>
            </td>
            <td>
                <%= job.created_at %>
            </td>
        </tr>
        <% end %>
    </tbody>


  </table>

</div>
