<% if flash.any? %>
	<% flash.each do |key, value| %>
		<div class="alert alert-dismissable alert-<%= convert(key) %>">
			<button class="close" data-dismiss="alert">&times;</button>
			<%= value %>
		</div>
	<% end %>
<% end %>