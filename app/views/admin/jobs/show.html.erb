<!-- <h1><%=@job.title%></h1>
<p>
  <%=simple_format(@job.description)%>
</p>

<table class="table">
  <thead>
   <tr>
      <th>应聘人</th>
      <th>下载简历</th>
      <th>投递时间</th>
    </tr>
  </thead>
  <tbody>
    <% @resumes.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= link_to "Download", post.attachment_url %></td>
        <td><%= post.created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->
