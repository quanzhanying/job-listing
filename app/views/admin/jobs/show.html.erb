<div class="col-md-12">
  <div class="group">
      <%= link_to("Edit", edit_admin_job_path(@job), class: "btn btn-default btn-sm pull-right")%>
  </div>
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>
  <p>Salary: <%= @job.wage_lower_bound %> ~ <%= @job.wage_upper_bound %></p>
</div>
